apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopscanvas-control-plane.fullname" . }}
  labels:
    {{- include "devopscanvas-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopscanvas-control-plane.selectorLabels" . | nindent 4 }}